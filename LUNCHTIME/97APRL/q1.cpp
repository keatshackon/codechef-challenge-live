#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main() {

	ll t, n, l, a, b, c;
	cin >> t;
	while (t--) {
		cin >> a >> b >> c;

		cout << (b - a) / c << "\n";
	}

	return 0;

}